/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
        "./applications/views/**/*.{php}"
    ],
    theme: {
        extend: {
            spacing: {
                // 'fluxo': '52rem',
            }
        },
    },
    plugins: [

    ],
}